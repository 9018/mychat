# Vox Collage Publish Director

Prepare `publish_log` with title, description, tags, chapters, thumbnail and final render references. Use `export_bundle` and optionally mirror only final deliverables to `/outputs/<project-id>/`. Do not copy intermediate MP4 files. Stop at the publish human gate and report the exact final path.
